import React from 'react';
import { View } from 'react-native';

export default function RootIndex() {
  // Navigation is handled by the RootLayout's NavigationHandler
  return <View style={{ flex: 1, backgroundColor: '#000' }} />;
}
